import { Component } from '@angular/core';
import { Router, ActivatedRoute, Params } from '@angular/router';


@Component({
  selector: 'sticker-upload',
  //we had used back-tick to  write multiline html in order to avoid string literal errors
  // ngModel used for two way data binding
  templateUrl: 'app/components/StickerUpload/sticker-upload.html',
  styleUrls:  []

		
})

export class StickerUploadComponent {
  

 }
